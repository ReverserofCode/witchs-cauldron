import SectionStatus, { type SectionStatusProps } from "./SectionStatus";

export type YouTubeSectionStatusProps = SectionStatusProps;

export default function YouTubeSectionStatus(props: SectionStatusProps) {
  return <SectionStatus {...props} />;
}
