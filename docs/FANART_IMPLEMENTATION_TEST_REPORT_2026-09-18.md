# 팬아트 승인형 게시 구현·검증 기록

작성일: 2026-09-18. 작업 상태: 구현 진행 중.

## 작업 범위

사용자가 승인한 `docs/superpowers/specs/2026-09-15-fanart-permission-workflow-design.md`를 구현한다. 계획은 `docs/superpowers/plans/2026-09-18-fanart-permission.md`에 있다.

- 기준: `origin/main`의 `f642625`.
- 별도 작업 트리와 `codex/fanart-permission-20260918` 브랜치 사용에 사용자 동의가 있었다.
- 원래 체크아웃과 미커밋 문서는 보존했다.
- 실제 작가 이미지·팬카페 로그인·메시지 발송·푸시·배포는 하지 않는다.

## 변경 전 기준 검증

| 검증 | 실행 결과 |
|---|---|
| `npm ci --no-audit --no-fund` | 설치 성공 |
| `npm test` | 29개 파일, 172개 통과·16개 조건부 건너뛰기 |
| `npm run lint` | 오류 0, 기존 경고 22 |
| 기존 potion DB 통합 테스트 | 전용 로컬 PostgreSQL에서 29개 통과 |

로컬 호스트 Node는 25.5.0이다. 운영 호환성은 별도의 Node22 Alpine Docker 빌드·실행으로 확인해야 한다. Docker Desktop은 처음 중지되어 있었으며 이 작업의 로컬 테스트를 위해 기동했다.

테스트 데이터베이스는 운영 환경과 분리된 로컬 PostgreSQL16 컨테이너 `witch-fanart-test-20260918`의 loopback 포트55439를 사용한다. 신규 DB 통합 테스트는 전용 schema로 격리하며 실제 운영 접속 정보를 사용하지 않는다.

## 운영상의 한계

- 기존 정적 팬아트는 자동으로 승인하지 않는다. 신규 카탈로그의 철회 기능은 기존 정적 URL에 소급 적용되지 않는다.
- 작가의 동의와 생성형 AI 미사용 확인은 운영자가 실제로 확인해야 한다. 시스템은 그 기록과 게시 조건을 검증할 뿐 진위를 자동 인증하지 않는다.
- 외부 수집·검색·메시지 자동 발송은 제외한다.
- 철회 후 새로운 파일 요청은 차단하지만 이미 다운로드된 사본을 회수할 수는 없다.

## 서버 구현 중간 검증

서버 구현 커밋: `6c37cec`. 화면/볼륨 통합 전의 중간 결과이며 최종 완료 판정은 아니다.

- 신규 팬아트 테스트 44개 통과: 도메인18, 저장소7, 이미지·HTTP11, 핸들러8.
- 전용 PostgreSQL을 설정한 전체 테스트: 216개 통과, 기존 조건부16개 건너뛰기.
- 타입 검사·프로덕션 빌드 통과. 신규 파일 lint 오류·경고0, 전체 기존 경고22 유지.
- Node22 Alpine Docker builder 이미지 빌드 통과.
- 최초 Docker 사전 빌드가 편집 중인 함수 인자 변경을 읽어 실패했다. 최종 커밋 전 수정·재검증했고 고정된 커밋으로 Docker 빌드를 다시 통과했다.

최종 구현 결과·테스트 수치·리뷰 결과는 전체 검증 완료 후 이 문서에 기록한다.
